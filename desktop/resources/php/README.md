Coloca aquí un runtime de PHP para Windows (por ejemplo `php.exe` + DLLs).

- Ruta esperada en el build final: `resources/php/php.exe`
- El wrapper lo usa automáticamente cuando la app está empaquetada.

Sugerencia: usa una distribución que incluya `pdo_sqlite`/`sqlite3` habilitados.
